{
  "name": "space-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.15.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nuxt": "^2.15.7"
  },
  "devDependencies": {
    "fibers": "^5.0.0",
    "push-dir": "^0.4.1",
    "sass": "^1.37.5",
    "sass-loader": "^10.2.0"
  }
}
